[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[context.production]
  branch = "react-dashboard"

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"


# SPA fallback: allow direct URLs such as /superadmin to load the React application.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
